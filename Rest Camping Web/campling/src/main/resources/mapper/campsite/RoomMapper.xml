<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.smu.camping.mapper.campsite.RoomMapper">
    <insert id = "createRoom" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO room(id, campsite_id, name, price, numRemains, maxHeadCnt, baseNumCars, baseHeadCnt)
        VALUES (#{id}, #{campsiteId}, #{name}, #{price}, #{numRemains}, #{maxHeadCnt}, #{baseNumCars}, #{baseHeadCnt})
    </insert>
</mapper>